name: nvidia-tegra-runtime # you probably want to 'snapcraft register <name>'
base: core24 # the base snap is the execution environment for this snap
version: '39.2' # just for humans, typically '1.2+git' or '1.3.2'
summary: NVIDIA Tegra firmware and core files for Jetson ORIN and THOR boards # 79 char long summary
description: |
  Install all necessary files to run CUDA toolkit on Ubuntu image

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

plugs:
  cuda:
    interface: content
    target: $SNAP/usr/lib/cuda-libs
apps:
  nvidia-smi:
    command: usr/sbin/nvidia-smi
    environment:
      LD_LIBRARY_PATH: $LD_LIBRARY_PATH:$SNAP/usr/lib/aarch64-linux-gnu/nvidia/:$SNAP/usr/lib/cuda-libs
    plugs:
      - hardware-observe
      - opengl
      - cuda

package-repositories:
  - type: apt
    components: [main]
    suites: [r39.2]
    key-id: 3C6D1FF3100C8C3ABB0869C0E6543461A9996195
    key-server: https://repo.download.nvidia.com/jetson/jetson-ota-public.asc
    url: https://repo.download.nvidia.com/jetson/som
    architectures: [arm64]
  - type: apt
    components: [main]
    suites: [r39.2]
    key-id: 3C6D1FF3100C8C3ABB0869C0E6543461A9996195
    key-server: https://repo.download.nvidia.com/jetson/jetson-ota-public.asc
    url: https://repo.download.nvidia.com/jetson/common
    architectures: [arm64]

parts:
  nvidia-tegra-runtime:
    # See 'snapcraft plugins'
    plugin: nil
    source: .
    stage-packages:
      - nvidia-l4t-3d-core
      - nvidia-l4t-adaruntime
      - nvidia-l4t-core
      - nvidia-l4t-cuda
      - nvidia-l4t-cuda-nvgpu
      - nvidia-l4t-cuda-openrm
      - nvidia-l4t-cuda-utils
      - nvidia-l4t-firmware
      - nvidia-l4t-firmware-nvgpu
      - nvidia-l4t-firmware-openrm
      - nvidia-l4t-gbm
      - nvidia-l4t-graphics-demos
      - nvidia-l4t-init
      - nvidia-l4t-libvulkan
      - nvidia-l4t-multimedia-utils
      - nvidia-l4t-nvml
      - nvidia-l4t-wayland
      - nvidia-l4t-weston
      - nvidia-l4t-x11

  scripts:
    plugin: dump
    source: scripts

slots:
  graphics-core22:
    interface: content
    read: [$SNAP]
  cuda-openrm:
    interface: content
    read: [$SNAP/opt/nvidia/l4t-gpu-libs/openrm]
  cuda-nvgpu:
    interface: content
    read: [$SNAP/opt/nvidia/l4t-gpu-libs/nvgpu]
